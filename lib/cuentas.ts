/** Contraseña fija para cuentas (proveedor o cliente) creadas desde el panel
 * admin — se comparte junto con el correo para que la persona entre por
 * primera vez. */
export const PASSWORD_CUENTA_DEFAULT = 'admin1234'
